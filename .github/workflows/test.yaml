name: DotNet-RestAPI V2 Build & Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  
jobs:
  test:
    runs-on: windows-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Setup .NET 
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: | 
          2.x.x
          2.2.x
          3.1.x
          5.0.x
          6.0.x
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1
    - name: Restore Tests
      run: dotnet restore Avalara.AvaTax.RestClient.Test.csproj
      working-directory: tests
    - name: Build Tests
      run: dotnet build Avalara.AvaTax.RestClient.Test.csproj --no-restore --configuration Release -p:SignAssembly=false
      working-directory: tests
    - name: Run Tests
      run: dotnet test Avalara.AvaTax.RestClient.Test.csproj --no-build --configuration Release
      env:
        SANDBOX_USERNAME: ${{ secrets.SANDBOX_USERNAME }}
        SANDBOX_PASSWORD: ${{ secrets.SANDBOX_PASSWORD }}
      working-directory: tests
